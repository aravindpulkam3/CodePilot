import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function Repositories() {
  return (
    <PlaceholderPage
      title="Repositories"
      description="Connect and browse the GitHub repositories your AI engineering workspace understands."
      note="GitHub repository listing and indexing are a future milestone — this page only reserves the route today."
    />
  );
}
